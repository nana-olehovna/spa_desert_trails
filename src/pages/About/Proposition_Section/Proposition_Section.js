import { t } from "../../../languages";

export const PropositionSection = () => {
    return `
      <section class="proposition">
        <article class="proposition_text">
          <p class="quote">${t("About.PropositionSection")}</p>
        </article>
      </section>
    `
}